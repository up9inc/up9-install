apiVersion: v1
kind: LimitRange
metadata:
  name: up9-limit-range
  namespace: {{ .Values.namespace }}
spec:
  limits:
    - type: Container
      default:
        memory: "512Mi"
        cpu: "200m"
      defaultRequest:
        memory: "100Mi"
        cpu: "50m"

